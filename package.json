{
  "name": "checkStream",
  "version": "1.0.0",
  "description": "Takes an RTSP url as a parameter then uses ffprobe to determine URL has an active stream.",
  "main": "checkStream.js",
  "scripts": {
    "test": "no test"
  },
  "bin": {
    "check-stream": "checkStream.js"
  },
  "keywords": [
    "checkStream",
    "rtsp",
    "utility"
  ],
  "author": "Rich Olson",
  "license": "MIT",
  "dependencies": {
    "child_process": "^1.0.2"
  }
}
